export const metadata = {
  title: "Sign Up | Anandbodh™",
  description: "Create your Anandbodh account to join wellness programs, community discussions, and guided health journeys.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function SignInLayout({ children }) {
  return children;
}
